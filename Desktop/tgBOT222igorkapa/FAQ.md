# ❓ Часто задаваемые вопросы (FAQ)

## 🚀 Запуск и установка

### Как запустить бота?
**Windows:**
1. Двойной клик на `start_bot.bat`

**Linux/macOS:**
```bash
python3 bot.py
```

### Какая версия Python нужна?
Python 3.8 или выше. Проверить:
```bash
python --version
```

### Как установить зависимости?
```bash
pip install -r requirements.txt
```

Или автоматически при запуске `start_bot.bat`

### Где взять токены?
- **Telegram Bot**: [@BotFather](https://t.me/BotFather)
- **Chat ID**: [@getmyid_bot](https://t.me/getmyid_bot)
- **Bit2Me API**: [bit2me.com](https://bit2me.com) → Account → API Keys

---

## ⚙️ Настройка

### Как изменить уровни падения?
Отредактируйте `config.py`:
```python
LEVELS = [
    {"level": 1, "drop": -5.0},  # Было -4.0
    {"level": 2, "drop": -10.0}, # Было -7.0
    # ...
]
```

### Как изменить интервал проверки?
```python
CHECK_INTERVAL = 30  # Было 60
```
⚠️ Учитывайте rate limits API

### Можно ли добавить свои монеты?
Бот автоматически мониторит ВСЕ доступные пары COIN/EUR на Bit2Me, отсортированные по объёму торгов (топ по ликвидности = топ по капитализации).

### Как отключить определённые монеты?
Добавьте фильтр в `market_monitor.py`:
```python
def filter_pairs(self):
    # ...
    blacklist = ["USDTEUR", "BUSDEUR"]
    filtered = [p for p in filtered if p not in blacklist]
```

---

## 📊 Работа бота

### Почему нет сигналов?
Это нормально! Сигналы появляются только при падениях:
- Level 1: -4% и больше
- Рынок может быть стабильным часами

### Как часто приходят сигналы?
Зависит от волатильности рынка:
- Спокойный рынок: 0-5 сигналов/день
- Активный рынок: 10-30 сигналов/день
- Крах: 50+ сигналов/день

### Максимум сигналов на монету?
**5 сигналов** за одну сессию падения (до RESET)

### Что такое RESET?
Сброс состояния монеты при:
- Росте +2% от минимума
- ИЛИ через 2 часа после последнего сигнала

После RESET можно получить новые 5 сигналов.

### Почему пришёл сигнал Level 2, но не было Level 1?
Цена упала сразу на -7%+ между проверками (60 сек).
Бот отправляет только первый пересечённый уровень.

---

## 🔧 Технические вопросы

### Где хранятся состояния?
В файле `pairs_state.json` в корне проекта.

### Можно ли удалить pairs_state.json?
Да, но:
- ✅ Бот создаст новый при старте
- ⚠️ Все состояния сбросятся
- ⚠️ Могут быть повторные сигналы

### Как проверить, что API работают?
```bash
python test_api.py
```
Или двойной клик на `test_api.bat`

### Сколько памяти потребляет бот?
~10-20 MB в зависимости от количества пар.

### Сколько трафика использует?
~1-2 MB/час при 50 парах и CHECK_INTERVAL=60.

### Можно ли запустить несколько ботов?
Да, но:
1. Разные папки или измените `STATE_FILE`
2. Разные токены/каналы (или одинаковые)
3. Учитывайте rate limits API

---

## 🐛 Проблемы и ошибки

### "Module requests not found"
Установите зависимости:
```bash
pip install requests
```

### "Telegram error 401 Unauthorized"
Неверный токен бота. Проверьте `TELEGRAM_BOT_TOKEN` в `config.py`.

### "Telegram error 400 Bad Request: chat not found"
Неверный Chat ID. Проверьте `TELEGRAM_CHAT_ID`:
- Должен начинаться с `-` для каналов/групп
- Для каналов: `-100` + ID канала

### "Bit2Me API error 403 Forbidden"
Неверный API ключ. Проверьте `BIT2ME_API_KEY` в `config.py`.


### Бот отправляет повторные сигналы
1. Убедитесь, что `pairs_state.json` существует
2. Проверьте загрузку: "✅ Загружено X состояний" в консоли
3. Если проблема сохраняется, удалите `pairs_state.json` и перезапустите

### Бот зависает на одной паре
Проблема с API той монеты:
- Бот пропустит её через 10 сек (timeout)
- В следующем цикле попробует снова

### Ошибка "JSON decode error"
Повреждён `pairs_state.json`:
```bash
# Удалите и перезапустите
rm pairs_state.json  # Linux/macOS
del pairs_state.json # Windows
```

---

## 📈 Оптимизация

### Как ускорить проверки?
Уменьшите `CHECK_INTERVAL`, но:
- Риск rate limits
- Больше нагрузка на API
- Больше трафика

Рекомендуется: 30-60 сек

### Как мониторить только ТОП-10?
Измените в `market_monitor.py`:
```python
def get_top_300_coins(self):
    params = {
        "per_page": 10,  # Было 300
        # ...
    }
```

### Можно ли добавить кэширование?
Да, добавьте Redis или Memcached для:
- Кэширования списка монет (обновлять раз в час)
- Кэширования доступных пар (обновлять раз в день)

### Как снизить нагрузку на API?
1. Увеличьте `CHECK_INTERVAL`
2. Уменьшите количество пар
3. Добавьте кэширование

---

## 🔒 Безопасность

### Как защитить токены?
1. ✅ Храните в `config.py` (не в коде)
2. ✅ Добавьте `config.py` в `.gitignore`
3. ✅ Не публикуйте скриншоты с токенами
4. ✅ Используйте переменные окружения (advanced)

### Можно ли использовать переменные окружения?
Да, измените `config.py`:
```python
import os
TELEGRAM_BOT_TOKEN = os.getenv("BOT_TOKEN", "default_token")
```

### Безопасно ли хранить токены в файле?
Да, если:
- Файл не попадает в Git (`.gitignore`)
- Сервер защищён от внешнего доступа
- Права доступа ограничены (Linux: `chmod 600 config.py`)

---

## 🌐 Развёртывание

### Как запустить на VPS 24/7?

**Linux (systemd):**
1. Создайте `/etc/systemd/system/cryptobot.service`
2. Добавьте:
```ini
[Unit]
Description=Crypto Signal Bot

[Service]
WorkingDirectory=/path/to/bot
ExecStart=/usr/bin/python3 bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```
3. `sudo systemctl enable cryptobot`
4. `sudo systemctl start cryptobot`

**Windows (Task Scheduler):**
1. Task Scheduler → Create Task
2. Trigger: At startup
3. Action: Start `start_bot.bat`

**Docker (advanced):**
```dockerfile
FROM python:3.9-slim
COPY . /app
WORKDIR /app
RUN pip install -r requirements.txt
CMD ["python", "bot.py"]
```

### Как просматривать логи на сервере?
**Systemd:**
```bash
journalctl -u cryptobot -f
```

**Screen/tmux:**
```bash
screen -S cryptobot
python bot.py
# Ctrl+A, D для detach
# screen -r cryptobot для возврата
```

---

## 💡 Продвинутое использование

### Мультиканальные сигналы
Измените `config.py`:
```python
TELEGRAM_CHAT_IDS = ["-1003623062179", "-1002222222222"]
```

И `telegram_sender.py`:
```python
for chat_id in TELEGRAM_CHAT_IDS:
    self.send_to_channel(chat_id, message)
```

### SELL-сигналы при росте
Добавьте в `config.py`:
```python
SELL_LEVELS = [
    {"level": 1, "growth": 5.0},
    {"level": 2, "growth": 10.0},
]
```

И логику в `market_monitor.py`

### Webhook вместо polling
Используйте Telegram Webhook для команд:
```python
from flask import Flask, request
app = Flask(__name__)

@app.route('/webhook', methods=['POST'])
def webhook():
    update = request.json
    # Обработка команд: /start, /stop, /status
```

### База данных вместо JSON
Замените `pairs_state.json` на SQLite/PostgreSQL:
```python
import sqlite3

class StateManager:
    def __init__(self):
        self.conn = sqlite3.connect('states.db')
        self.create_table()
```

---

## 📚 Дополнительные ресурсы

### Документация API
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [CoinGecko API](https://www.coingecko.com/en/api)
- [Bit2Me API](https://bit2me.com/api-docs)

### Полезные библиотеки
- `python-telegram-bot` - более функциональная работа с Telegram
- `ccxt` - универсальная библиотека для бирж
- `pandas` - анализ данных
- `matplotlib` - графики

### Обучение
- [Python для начинающих](https://www.python.org/about/gettingstarted/)
- [API что это](https://restfulapi.net/)
- [Telegram боты](https://core.telegram.org/bots)

---

## 🤝 Вклад в проект

### Как предложить улучшение?
1. Fork репозитория
2. Создайте ветку: `git checkout -b feature-name`
3. Commit: `git commit -m "Add feature"`
4. Push: `git push origin feature-name`
5. Create Pull Request

### Как сообщить о баге?
Создайте Issue с:
- Описанием проблемы
- Шагами для воспроизведения
- Ожидаемым поведением
- Логами из консоли

---

## 📞 Поддержка

Если вопрос не решён:
1. Проверьте документацию (README, TECHNICAL, QUICKSTART)
2. Запустите `test_api.py`
3. Проверьте логи в консоли
4. Создайте Issue на GitHub

**⚠️ Disclaimer**: Бот не является финансовым советником.
Используйте на свой риск.

