# 🤖 Crypto Signal Bot

Бот для мониторинга криптовалютного рынка и отправки BUY-сигналов в Telegram при резких падениях цены.

## 📋 Возможности

- ✅ Мониторинг всех доступных пар COIN/EUR на Bit2Me
- ✅ Автоматическая сортировка по объёму торгов (ТОП-300)
- ✅ 5 уровней сигналов: -4%, -7%, -11%, -16%, -22%
- ✅ Автоматический RESET при росте +2% или через 2 часа
- ✅ Сохранение состояний при рестарте
- ✅ Защита от спама (1 сигнал на уровень)

## 🚀 Установка

1. Установите Python 3.8+
2. Установите зависимости:

```bash
pip install -r requirements.txt
```

## ▶️ Запуск

```bash
python bot.py
```

Для остановки нажмите `Ctrl+C`

## 📊 Логика работы

### Локальный максимум
- Хранится за последние 4 часа
- Обновляется при росте цены
- Сбрасывается при RESET

### Уровни сигналов
1. Level 1: -4%
2. Level 2: -7%
3. Level 3: -11%
4. Level 4: -16%
5. Level 5: -22%

Каждый уровень срабатывает только 1 раз за сессию падения.

### RESET условия
Сброс происходит при:
- Росте цены на +2% от локального минимума
- ИЛИ через 2 часа после последнего сигнала

После RESET все уровни обнуляются.

## 💾 Сохранение состояний

Все состояния пар сохраняются в `pairs_state.json`:
- Локальный максимум
- Локальный минимум
- Сработавшие уровни
- Время последнего сигнала

При рестарте бота состояния загружаются автоматически.

## 📱 Формат сигнала

```
🚀 SOL/EUR | -11.0% | COMPRAR
⚠️ No es recomendación financiera.
```

Кнопка COMPRAR ведёт на страницу покупки на Bit2Me.

## ⚙️ Настройки

Все настройки в файле `config.py`:
- `CHECK_INTERVAL` - интервал проверки (60 сек)
- `LOCAL_MAX_PERIOD` - период локального максимума (4 часа)
- `RESET_TIME` - время до автосброса (2 часа)
- `RESET_GROWTH_PERCENT` - процент роста для сброса (+2%)
- `LEVELS` - уровни падения для сигналов

## 🔧 Структура проекта

```
.
├── bot.py              # Главный файл бота
├── config.py           # Конфигурация
├── state_manager.py    # Управление состояниями
├── market_monitor.py   # Мониторинг рынка
├── telegram_sender.py  # Отправка в Telegram
├── requirements.txt    # Зависимости
├── pairs_state.json    # Состояния пар (создаётся автоматически)
└── README.md           # Документация
```

## 📝 Логи

Бот выводит подробные логи в консоль:
- 🆕 Новая пара
- 📈 Новый максимум
- 🔄 RESET состояния
- 🚨 Сигнал на уровне
- ✅ Статус операций

## ⚠️ Важно

- Требуется стабильное интернет-соединение
- API ключи должны быть валидными
- Рекомендуется запускать на VPS для непрерывной работы

