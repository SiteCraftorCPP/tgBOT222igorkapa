# 📚 Техническая документация

## Архитектура бота

### Модули

#### 1. `config.py` - Конфигурация
Все настройки бота в одном месте:
```python
TELEGRAM_BOT_TOKEN      # Токен Telegram бота
TELEGRAM_CHAT_ID        # ID канала для сигналов
BIT2ME_API_KEY          # API ключ биржи
CHECK_INTERVAL          # Интервал проверки (60 сек)
LEVELS                  # Уровни падения для сигналов
```

#### 2. `state_manager.py` - Управление состояниями
Отвечает за сохранение и загрузку состояний пар:

**Структура состояния пары:**
```json
{
  "BTCEUR": {
    "local_max": 45000.0,           // Локальный максимум за 4 часа
    "local_min": 42500.0,           // Минимум текущей сессии падения
    "triggered_levels": [1, 2],     // Сработавшие уровни
    "last_signal_time": 1704628800, // Время последнего сигнала (Unix)
    "last_price": 43200.0,          // Последняя цена
    "last_update": 1704628900       // Время последнего обновления
  }
}
```

**Методы:**
- `load_states()` - Загрузка из `pairs_state.json`
- `save_states()` - Сохранение в файл
- `get_state(pair)` - Получить состояние пары
- `update_state(pair, **kwargs)` - Обновить состояние
- `reset_state(pair, new_price)` - Сброс состояния
- `should_reset(pair, price, time)` - Проверка условий RESET
- `add_triggered_level(pair, level, time)` - Добавить уровень

#### 3. `market_monitor.py` - Мониторинг рынка
Работа с API бирж:

**Методы:**
- `get_all_pairs_with_volume()` - Получить все пары с Bit2Me с данными о объёме
- `filter_pairs()` - Сортировка по объёму торгов (ТОП-300)
- `get_current_price(pair)` - Текущая цена пары
- `check_levels(pair, price, max, levels)` - Проверка уровней

**API запросы:**
```
Bit2Me: GET /v1/ticker/book (список пар + объёмы)
Bit2Me: GET /v1/ticker/{pair} (текущая цена)
```

#### 4. `telegram_sender.py` - Отправка в Telegram
Отправка сигналов и статусов:

**Методы:**
- `send_signal(pair, drop_percent)` - BUY-сигнал
- `send_status(message)` - Статусное сообщение

**Формат сигнала:**
```
🚀 SOL/EUR | -11.0% | COMPRAR
⚠️ No es recomendación financiera.
[Inline кнопка: COMPRAR → https://bit2me.com/es/precio/sol]
```

#### 5. `bot.py` - Главный модуль
Координирует работу всех модулей:

**Класс `CryptoSignalBot`:**
- `start()` - Запуск бота
- `main_loop(pairs)` - Основной цикл мониторинга
- `check_pair(pair, time)` - Проверка одной пары
- `stop()` - Остановка и сохранение

## Алгоритм работы

### Инициализация
1. Загрузка сохранённых состояний из файла
2. Получение всех пар COIN/EUR с Bit2Me
3. Сортировка по объёму торгов (ТОП-300)
4. Отправка статуса запуска в Telegram

### Основной цикл (каждые 60 сек)
```
ДЛЯ КАЖДОЙ пары:
  1. Получить текущую цену
  2. Если пара новая → инициализировать состояние
  3. Проверить условия RESET → сбросить если нужно
  4. Обновить локальный максимум (если цена выше)
  5. Обновить локальный минимум (если цена ниже)
  6. Проверить пересечение уровней падения
  7. Если новый уровень → отправить сигнал
  8. Сохранить состояние
```

### Логика RESET

**Условие 1: Рост +2% от минимума**
```python
if (current_price - local_min) / local_min * 100 >= 2.0:
    RESET()
```

**Условие 2: Прошло 2 часа с последнего сигнала**
```python
if current_time - last_signal_time >= 7200:  # 2 часа
    RESET()
```

**Действия при RESET:**
- Обнулить все сработавшие уровни
- Установить новый локальный максимум = текущая цена
- Очистить локальный минимум
- Очистить время последнего сигнала

### Проверка уровней

```python
drop_percent = (current_price - local_max) / local_max * 100

ДЛЯ КАЖДОГО уровня (-4%, -7%, -11%, -16%, -22%):
  ЕСЛИ drop_percent <= уровень И уровень НЕ сработал:
    → Отправить сигнал
    → Добавить уровень в triggered_levels
    → Сохранить время сигнала
```

**Важно:**
- Проверка идёт **по порядку** (от -4% до -22%)
- Уровень срабатывает **только 1 раз** до RESET
- Можно получить **максимум 5 сигналов** за сессию падения

## Обработка ошибок

### Ошибки API
- Retry на следующем цикле (60 сек)
- Логирование ошибки в консоль
- Пара пропускается, остальные обрабатываются

### Ошибки сохранения состояний
- Логирование, но работа продолжается
- При рестарте загрузятся последние валидные данные

### Ошибки Telegram
- Логирование
- Бот продолжает мониторинг
- Повторная попытка на следующем сигнале

## Производительность

### Оптимизации
- Задержка 0.5 сек между запросами к API
- Локальное кэширование состояний в RAM
- Сохранение в файл только при изменениях

### Нагрузка
```
50 пар × 1 запрос/60 сек = ~1 запрос/сек
Потребление RAM: ~10-20 MB
Трафик: ~1-2 MB/час
```

## Безопасность

### Защита API ключей
- Хранятся в `config.py`
- Добавить в `.gitignore` перед коммитом в Git
- Не показывать в логах

### Антиспам
- 1 сигнал на уровень
- Минимальный интервал между сигналами: время падения
- RESET после восстановления или через 2 часа

## Расширение функционала

### Добавление новых уровней
```python
# config.py
LEVELS = [
    {"level": 1, "drop": -4.0},
    {"level": 6, "drop": -30.0},  # Новый уровень
]
```

### Изменение интервала проверки
```python
# config.py
CHECK_INTERVAL = 30  # Проверка каждые 30 сек
```

### Добавление новых условий RESET
```python
# state_manager.py, метод should_reset()
if condition_3:
    return True
```

### Мультиканальные сигналы
```python
# config.py
TELEGRAM_CHAT_IDS = ["-1003623062179", "-1002222222222"]

# telegram_sender.py
for chat_id in TELEGRAM_CHAT_IDS:
    send_to_channel(chat_id, message)
```

## Мониторинг и отладка

### Логи
Все важные события выводятся в консоль:
- `🆕` - Новая пара
- `📈` - Новый максимум
- `🔄` - RESET
- `🚨` - Сигнал
- `❌` - Ошибка
- `✅` - Успех

### Проверка состояний
Просмотр `pairs_state.json` в любой момент:
```json
{
  "BTCEUR": {
    "local_max": 45000.0,
    "triggered_levels": [1, 2]
  }
}
```

### Тестирование API
```bash
python test_api.py
```

Проверяет:
- Telegram Bot API
- Отправку сообщений
- CoinGecko API
- Bit2Me API

## FAQ

**Q: Можно ли запустить несколько ботов?**
A: Да, но измените `STATE_FILE` для каждого инстанса.

**Q: Как добавить поддержку USD пар?**
A: Измените фильтр в `market_monitor.py`:
```python
if symbol.endswith("USD"):
```

**Q: Бот шлёт повторные сигналы**
A: Проверьте наличие `pairs_state.json` и его загрузку при старте.

**Q: Как ускорить проверки?**
A: Уменьшите `CHECK_INTERVAL`, но учтите rate limits API.

**Q: Можно ли добавить стоп-лосс сигналы?**
A: Да, добавьте логику в `check_pair()` для проверки больших падений.

