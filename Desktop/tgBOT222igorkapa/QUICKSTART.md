# 🚀 Быстрый старт

## Запуск бота (Windows)

1. **Двойной клик** на `start_bot.bat`
2. Готово! Бот запущен.

## Остановка бота

Нажмите `Ctrl+C` в окне консоли

## Проверка работы

После запуска бот:
1. ✅ Загрузит сохранённые состояния (если есть)
2. ✅ Получит список TOP-300 монет
3. ✅ Отфильтрует доступные пары на Bit2Me
4. ✅ Отправит статус запуска в Telegram-канал
5. ✅ Начнёт мониторинг каждые 60 секунд

## Что будет в консоли

```
============================================
🤖 Бот мониторинга криптовалют запущен
============================================

📊 Получение списка торговых пар с Bit2Me...
✅ Получено 250 пар COIN/EUR с Bit2Me
✅ Отобрано 250 топ-пар по объёму торгов
   Примеры: BTCEUR, ETHEUR, BNBEUR, XRPEUR, ADAEUR...

✅ Мониторим 250 пар
⏱️ Интервал проверки: 60 сек

============================================
🔄 Цикл #1 | 14:23:45
============================================
🆕 BTCEUR: инициализация, цена=45000.00
🆕 ETHEUR: инициализация, цена=2300.00
...
```

## Пример сигнала в Telegram

```
🚀 SOL/EUR | -11.0% | COMPRAR
⚠️ No es recomendación financiera.

[Кнопка: COMPRAR] → ссылка на Bit2Me
```

## Файлы бота

- `pairs_state.json` - сохранённые состояния (создаётся автоматически)
- Логи выводятся в консоль

## Автозапуск при рестарте ПК (опционально)

1. Нажми `Win+R`
2. Введи `shell:startup`
3. Создай ярлык `start_bot.bat` в открывшейся папке

## Проблемы?

### Бот не запускается
- Проверь, установлен ли Python 3.8+
- Запусти: `pip install -r requirements.txt`

### Нет сигналов
- Нормально! Сигналы только при падениях -4% и больше
- Проверь консоль на ошибки API

### Повторные сигналы
- Не должно быть! Состояния сохраняются в `pairs_state.json`
- Если проблема есть - удали файл и перезапусти бота

## Настройки

Редактируй `config.py`:
```python
CHECK_INTERVAL = 60  # Частота проверки (сек)
LEVELS = [...]       # Уровни падения
```

## Запуск на VPS (24/7)

Для постоянной работы используй VPS с Windows или Linux:

**Linux:**
```bash
nohup python3 bot.py &
```

**Windows (Task Scheduler):**
1. Открой Task Scheduler
2. Create Basic Task
3. Trigger: At startup
4. Action: Start a program → `start_bot.bat`

